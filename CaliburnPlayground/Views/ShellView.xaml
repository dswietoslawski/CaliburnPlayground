<Window x:Class="CaliburnPlayground.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cal="http://www.caliburnproject.org"
        xmlns:local="clr-namespace:CaliburnPlayground.Views"
        mc:Ignorable="d"
        Title="ShellView" Height="720" Width="1280">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="ToggleToDoItems" Content="Toggle ToDos"></Button>
                <Button x:Name="CheckIfSubsOk" Content="Send message"></Button>
            </StackPanel>

            <ContentControl Grid.Row="1" x:Name="ActiveItem"></ContentControl>
    </Grid>
</Window>
